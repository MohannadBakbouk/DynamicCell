# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'

target 'DynamicCell' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DynamicCell
  pod 'Kingfisher', '~> 6.0'
  pod 'Fakery'

  target 'DynamicCellTests' do
    inherit! :search_paths
    # Pods for testing
    
  end

  target 'DynamicCellUITests' do
    # Pods for testing
  end

end
